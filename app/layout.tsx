import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MELODIC STUDIO",
description: "MELODIC STUDIOS is your home for emotional and dark-style beats. Discover fresh instrumentals updated daily — crafted to inspire artists, producers, and dreamers around the world.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link rel="icon" href="/Favicon/favicon.ico" type="image/x-icon" />
        {/* You can also use .png: <link rel="icon" href="/Favicon/favicon.png" type="image/png" /> */}
        <title>MELODIC STUDIOS</title>
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
